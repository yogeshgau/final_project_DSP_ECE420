<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:paddingLeft="24dp"
    android:paddingRight="24dp"
    android:paddingTop="32dp"
    android:paddingBottom="32dp"
    android:background="#FAFAFA">

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rep Counter"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#212121"
        android:layout_marginTop="16dp" />

    <!-- Large rep count display -->
    <TextView
        android:id="@+id/textRepCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="--"
        android:textSize="100sp"
        android:textStyle="bold"
        android:textColor="#1565C0"
        android:layout_marginTop="32dp" />

    <!-- Status line -->
    <TextView
        android:id="@+id/textStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Press Start to begin recording"
        android:textSize="15sp"
        android:textColor="#616161"
        android:layout_marginTop="12dp" />

    <!-- Start / Stop buttons -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="56dp">

        <Button
            android:id="@+id/buttonStart"
            android:layout_width="130dp"
            android:layout_height="wrap_content"
            android:text="Start"
            android:layout_marginEnd="20dp" />

        <Button
            android:id="@+id/buttonStop"
            android:layout_width="130dp"
            android:layout_height="wrap_content"
            android:text="Stop" />

    </LinearLayout>

    <!-- Active signal mode label -->
    <TextView
        android:id="@+id/textMode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Signal: Accel Jerk"
        android:textSize="13sp"
        android:textColor="#9E9E9E"
        android:layout_marginTop="32dp" />

    <!-- Toggle between accel-jerk and gyro-jerk -->
    <Button
        android:id="@+id/buttonMode"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:text="Switch to Gyro Jerk"
        android:layout_marginTop="6dp" />

</LinearLayout>
